- provide(:title, '部門別ランキング')
%h3.center_mini_device
  = link_to users_path, :class => "badge_send_button_top" do
    %span.minismile= image_tag asset_path "smile.png"
    バッジを贈る！！
%h1 贈ったバッジランキング（今月）
.container-fluid
  .row
    .col-sm-2
    .col-sm-10
      - if @badgeposts.present?
        %ul.ranking
          - ranking_count = 1
          - @badgeposts.each do |badgepost|
            %li
              %span.rank
                = ranking_count.to_s + " 位"
              - ranking_count += 1
              .bumon_name= @bumonname_hash[badgepost.bumon_id] if @bumonname_hash.has_key?(badgepost.bumon_id)
              %span.badge_count
                = badgepost.cnt
                個
            %br/
            %br/
            %br/
